{
    "name": "write_curl_to_file",
    "description": "Use this tool to write the curl commands to file",
    "input_schema": {
      "type": "object",
      "properties": {
        "file_name": { 
            "type": "string", 
            "description": "The name of the file to write the details to, without any extensions" 
        },
        "curl_command": { 
            "type": "string", 
            "description": "The curl command to write to the file" 
        }
      },
      "required": ["file_name", "curl_command"]
    }
}
