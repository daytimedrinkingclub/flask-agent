{
    "name": "web_search",
    "description": "This tool is very useful to learn about the latest api endpoints so we can use the information for creating custom actions or instructions, additionally you can also discover common use cases for support by industry, users business type",
    "input_schema": {
      "type": "object",
      "properties": {
        "query": { 
            "type": "string", 
            "description": "The query to search for, always try to use this query when you need to search for something on the web"
        }
      },
      "required": ["query"]
    }
}